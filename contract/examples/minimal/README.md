# Minimal dataset

The smallest valid dataset: the baseline that every schema ticket extends.

- **C3:** `manifest.json`, `sources.json`
- **C4–C6:** add `airlines.json`, `fleets/`, `search-docs.json`, and update `manifest.counts` to match

The sources are real services, so the attribution fields show realistic values. No records cite them yet.
