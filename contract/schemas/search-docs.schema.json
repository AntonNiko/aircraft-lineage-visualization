{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aircraft-lineage.example/contract/search-docs.schema.json",
  "title": "Search docs",
  "description": "Raw documents the app indexes for search (label and terms). The app builds the index in the browser, so this file doesn't depend on a search library. Rules JSON Schema can't check: every airline in airlines.json and every aircraft in fleets/*.json has exactly one doc; ids and airline_ids reference existing entries; an aircraft doc's airline_id is the airline whose fleet file lists it.",
  "x-contract-status": "stable",
  "type": "object",
  "additionalProperties": false,
  "required": ["docs"],
  "properties": {
    "docs": {
      "description": "All search documents, sorted by kind, then id (ascending). Each (kind, id) pair appears once.",
      "type": "array",
      "items": {
        "oneOf": [{ "$ref": "#/$defs/aircraftDoc" }, { "$ref": "#/$defs/airlineDoc" }]
      }
    }
  },
  "$defs": {
    "aircraftDoc": {
      "title": "AircraftSearchDoc",
      "description": "An aircraft the user can search for.",
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "id", "airline_id", "label", "terms"],
      "properties": {
        "kind": {
          "description": "Document type.",
          "type": "string",
          "const": "aircraft"
        },
        "id": {
          "description": "Airframe the result opens.",
          "$ref": "defs.schema.json#/$defs/airframeId"
        },
        "airline_id": {
          "description": "Airline whose fleet file lists the aircraft, so selecting the result loads only that fleet. Null if the aircraft isn't in any current fleet.",
          "oneOf": [{ "$ref": "defs.schema.json#/$defs/airlineId" }, { "type": "null" }]
        },
        "label": {
          "$ref": "#/$defs/label"
        },
        "terms": {
          "description": "Strings the aircraft can be found by: at least its current registration and MSN when known, plus past registrations and ICAO 24-bit addresses (uppercase hex).",
          "$ref": "#/$defs/terms"
        }
      }
    },
    "airlineDoc": {
      "title": "AirlineSearchDoc",
      "description": "An airline the user can search for.",
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "id", "label", "terms"],
      "properties": {
        "kind": {
          "description": "Document type.",
          "type": "string",
          "const": "airline"
        },
        "id": {
          "description": "Airline the result opens.",
          "$ref": "defs.schema.json#/$defs/airlineId"
        },
        "label": {
          "$ref": "#/$defs/label"
        },
        "terms": {
          "description": "Strings the airline can be found by: at least its name, plus ICAO and IATA codes when known and any former names.",
          "$ref": "#/$defs/terms"
        }
      }
    },
    "label": {
      "title": "SearchLabel",
      "description": "Text shown in the results list, e.g. 'D-AIZA · A320 · Lufthansa' for an aircraft or 'Lufthansa (LH / DLH)' for an airline.",
      "type": "string",
      "minLength": 1,
      "maxLength": 200
    },
    "terms": {
      "title": "SearchTerms",
      "description": "Search terms: at least one, no duplicates, sorted ascending by code point. Each term is 1-100 characters with no leading or trailing whitespace.",
      "type": "array",
      "minItems": 1,
      "maxItems": 50,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1,
        "maxLength": 100,
        "pattern": "^[^\\s](.*[^\\s])?$"
      }
    }
  }
}
